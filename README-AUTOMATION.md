## GitHub Sync Script

The `github-sync.sh` script synchronizes the mainline branch between GitHub and origin remotes. It verifies prerequisites (branch, authentication, clean working tree), fetches from GitHub, merges changes, and pushes to both remotes.
